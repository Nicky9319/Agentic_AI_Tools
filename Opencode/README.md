## Configuration Locations

- **Global configuration:**  
    Use the `~/.config/opencode/` directory.

- **Folder-specific configuration:**  
    Use the `./opencode/` directory.  
    Create an `opencode.json` file in the respective folder for configuring opencode itself by default.